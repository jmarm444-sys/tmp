# notes-log.md

Raw command output, tool traces, and work-in-progress notes.

**Order:** newest first. On each update, **insert** a new `## …` section **immediately after the `---` line below** (above older `##` sections).

---

## 2026-05-01 — SESSION.md handoff

Created `SESSION.md` at repo root: one-page resume pointer (holdings parser, `notes-log.md`, example commands, Chase vs USB split). User asked to bookmark Cursor continuity via repo file.

## 2026-05-01 — setup

User request: keep **raw logs** in this file as work proceeds; later updated to **newest at top**.

Convention:
- Paste **full** terminal / relevant tool output when useful (not summarized).
- One-line context before each raw block if needed.
